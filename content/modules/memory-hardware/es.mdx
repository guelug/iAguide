## Tres máquinas, tres historias de memoria

<VisualSlot caption="GPU desmontable: inspecciona sus componentes y calcula la memoria de pesos, caché KV y reserva con parámetros explícitos." />

- **NVIDIA discreta (y similares)** — pesos y KV quieren vivir en <Term id="vram">VRAM</Term>. La RAM del host es otro planeta. Cruzar <Term id="pcie">PCIe</Term> para traer una capa es mucho más lento que quedarse en el dispositivo. Descargar capas a RAM (llama.cpp `-ngl`) funciona; es un compromiso.
- **<Term id="unified-memory">Memoria unificada</Term> de Apple Silicon** — CPU y GPU comparten un pool. No hay copia PCIe del modelo entero para "ponerlo en la GPU". Sigues teniendo un pool *finito* (los 16 / 32 / 64 / 128 GB que compraste) y límites de **ancho de banda**. Unificado no significa infinito.
- **RAM + CPU** — llama.cpp corre solo en RAM. La capacidad es más fácil; el <Term id="bandwidth">ancho de banda</Term> es peor. Vale para lotes y experimentos; duele como chat ágil.

## NVIDIA y Apple Max: anatomía de dos arquitecturas

<VisualSlot id="memory-hardware:4" caption="NVIDIA y Apple Max: componentes, memoria y conexiones en un despiece interactivo." />

Una GPU dedicada tiene su propia memoria: la **GeForce RTX 5090 incorpora 32 GB de GDDR7**. La CPU del equipo utiliza otra RAM, y PCIe conecta ambos dominios. La maquetación de esta lámina es didáctica: las piezas permiten entender conexiones y funciones, no reproducen el plano de una tarjeta comercial.

En un Mac, **CPU y GPU del M4 Max comparten memoria unificada**. La variante de **16 núcleos CPU y 40 GPU** admite hasta **128 GB** y publica **546 GB/s** de ancho de banda; la variante de **14 CPU y 32 GPU** publica **410 GB/s**. Compartir memoria evita la necesidad de una copia del modelo entre RAM y VRAM separadas, pero sigue habiendo transferencias desde el SSD, coordinación de accesos y trabajo del motor. La capacidad también la utilizan macOS y otras aplicaciones.

Son configuraciones identificadas, no una afirmación de que todos los chips Max sean iguales. Fuentes: [NVIDIA RTX 5090](https://www.nvidia.com/en-us/geforce/graphics-cards/50-series/rtx-5090/) y [Apple M4 Max](https://support.apple.com/en-mide/121553).

## Del archivo al token: carga, prefill y decode

<VisualSlot id="memory-hardware:5" caption="Sigue los datos desde el SSD hasta la generación, en NVIDIA y en memoria unificada." />

1. **Carga**. El archivo de pesos sale del SSD. En la ruta convencional de una GPU dedicada pasa por memoria del equipo y se transfiere a VRAM. En Apple se carga en el espacio compartido que puede utilizar la GPU. Hay otras rutas de entrada y estrategias de descarga; la escena muestra este caso habitual.
2. **Prefill**. El motor procesa los tokens de entrada y construye sus claves y valores. La KV se escribe en memoria; pesos, activaciones y temporales también participan. Los tamaños de bloque dependen del motor.
3. **Decode**. Cada paso usa pesos y caché previa para calcular el siguiente token, y añade su nueva KV. Con un modelo denso y lote pequeño, el tráfico de memoria suele ser un factor importante. Con otros lotes o arquitecturas, el equilibrio cambia.

Los paquetes y las duraciones animadas sirven para seguir el proceso. **No representan velocidades medidas** ni comparan latencias reales entre equipos.

## Capacidad frente a ancho de banda, las dos preguntas

<VisualSlot id="memory-hardware:6" caption="Capacidad y ancho de banda: RTX 5090, M4 Max y M3 Max con una carga editable." />

**Capacidad** responde a cuánto puedes mantener en memoria. **Ancho de banda** responde a cuántos bytes puedes mover por segundo. Ninguna de las dos cifras, por sí sola, determina la velocidad del modelo.

| Configuración de referencia | Memoria | Ancho de banda publicado |
| --- | --- | --- |
| GeForce RTX 5090 | 32 GB GDDR7 dedicados | 1.792 GB/s |
| M4 Max, 16 CPU / 40 GPU, configuración de 128 GB | 128 GB compartidos | 546 GB/s |
| M3 Max, 16 CPU / 40 GPU, configuración de 128 GB | 128 GB compartidos | 400 GB/s |

El M3 Max de 14 CPU y 30 GPU tiene **300 GB/s**; no deben trasladarse los 400 GB/s de la variante superior a cualquier Mac con ese nombre. La tabla tampoco compara el ancho de banda de VRAM con el de PCIe: son enlaces distintos. Fuentes: [tabla de NVIDIA Blackwell](https://images.nvidia.com/aem-dam/Solutions/geforce/blackwell/nvidia-rtx-blackwell-gpu-architecture.pdf), [ficha de M4 Max](https://support.apple.com/en-mide/121553) y [ficha de M3 Max](https://support.apple.com/en-us/117737).

<Callout>
**Una cuenta de lectura no es un benchmark.** Leer 4 GB a 400 GB/s daría un mínimo ideal de 10 ms. Eso no incluye cómputo, lectura de KV, sincronización, cachés ni eficiencia del motor. No equivale a una promesa de tokens por segundo. En memoria unificada, otros consumidores también comparten el caudal.
</Callout>

## Presupuesto del modelo: pesos, KV y reserva

<VisualSlot id="memory-hardware:3" />

## Lo que vive en memoria: tres inquilinos

Un inference server carga tres cosas, no una:

1. **Pesos del modelo** — el archivo. Inmutable durante la sesión. Dense 8B FP16: 16 GB. A3B 48B FP16: 96 GB. A3B 48B Q4: ~24 GB. **Es un impuesto fijo**: si no cabe, no arranca.
2. **<Term id="kv-cache">Caché KV</Term>** — la memoria de trabajo de la atención. Crece con la longitud del contexto y con el número de secuencias concurrentes. Fórmula aproximada:
   `bytes_KV ≈ 2 × capas × cabezas_KV × head_dim × bytes_por_elemento_KV × tokens_de_contexto × secuencias`
   En la práctica: un 8B denso con contexto 8k ocupa ~0.5-1 GB de KV; con 128k, ~8-16 GB. **Es el segundo inquilino** y el que más se subestima.
3. **Runtime, activaciones, fragmentación** — el runtime de CUDA/Metal, las activaciones pico del prefill, los buffers temporales, el overhead del allocator. Suma entre 1 y 4 GB según el motor y la configuración. **Es el inquilino invisible** que nunca aparece en `params × bytes`.

<Callout>
**Regla práctica para dimensionar**: `memoria_total ≈ pesos + KV_pico + 3 GB de overhead`. Si esa suma supera tu VRAM, falla en producción aunque los pesos solos quepan.
</Callout>

## Cómo crece la KV con el contexto

La KV caché es proporcional a **longitud de contexto × número de capas × cabezas × head_dim**. Lo que cambia entre arquitecturas:

- **MHA** (multi-head attention clásico) — la KV se almacena por cabeza. Cara.
- **MQA / GQA** (multi-query / grouped-query) — varias cabezas Q comparten una cabeza KV. **Reduce la KV por un factor de 4-8.** Por eso un Llama 3 con GQA aguanta contextos largos donde un MHA antiguo no.
- **Sliding window attention** (Mistral) — la KV solo cubre los últimos N tokens. Capacidad fija, contexto "aparentemente" largo.
- **Paged KV** (vLLM, llama.cpp) — la memoria se asigna en páginas, no en bloque contiguo. Permite más usuarios concurrentes y reduce fragmentación.

<Compare>
<Side title="Dense 8B, contexto 8k">
Pesos ~16 GB FP16. KV ~0.5-1 GB con GQA. Cabe en una 4090 con margen.
</Side>
<Side title="Dense 8B, contexto 128k">
Pesos ~16 GB. KV ~8-16 GB con GQA. Cabe en una A100 80 GB, no en una 4090.
</Side>
</Compare>

## El caso A3B: la memoria que miente la cuenta

A3B (48B totales / 3B activos) es el ejemplo donde marketing y realidad chocan:

- **El cómputo** por token es comparable a un dense 3B. Decode rápido.
- **La memoria** que tienes que cargar son los 48B de expertos completos. En FP16: 96 GB. En Q4_K_M: ~24-28 GB. En Q2_K: ~12-14 GB.

El "active 3B" es **una propiedad de cómputo**, no de memoria. En cada paso de decode, solo 3B hacen trabajo; el resto está sentado en memoria sin hacer nada, pero **tiene que estar ahí** porque el router puede mandarte a cualquiera.

<Callout>
**MoE nunca reduces memoria con "active".** Si tienes un A3B 48B y te dicen "cabe en 12 GB porque activas 3B", te están vendiendo. Mira `ls -lh` del archivo, mira la tabla de modelos, mira los benchmarks con medición de VRAM. La cifra de activos sirve para *predecir velocidad*, no para *dimensionar capacidad*.
</Callout>

<Myth>
"Un MoE 8x7B ocupa lo mismo que un dense 7B porque solo se usan dos expertos a la vez." Falso. Cada uno de los 8 expertos tiene sus 7B de parámetros. Si todos están cuantizados a 4 bits, el archivo es ~8 × 7B × 0.5 bytes ≈ 28 GB. Un dense 7B Q4 serían ~3.5 GB. **Ocho veces más memoria**, no dos.
</Myth>

## Dónde mueren los modelos: las tres trampas reales

Trampa 1: **los pesos caben, la KV no.** Caso típico: 8B en 4090 (24 GB), contexto largo. Pesos 4 GB en Q4 caben. KV a 64k son 8 GB. Sobran 12 GB… hasta que dos usuarios concurrentes duplican la KV. Mueres en producción, no en tu prueba.

Trampa 2: **la VRAM parece libre pero el contexto CUDA se la come.** El runtime reserva 1-3 GB para sí mismo. Las activaciones del prefill pueden picar a varios GB en contextos largos. La fragmentación del allocator te roba otro tanto. Tu `nvidia-smi` dice 2 GB libres, tu modelo de 4 GB no carga.

Trampa 3: **confundir memoria instalada con memoria disponible para el modelo.** En un Mac de 64 GB, el sistema y las aplicaciones comparten ese presupuesto con la GPU. La reserva necesaria varía: mídela en tu sesión. El rendimiento también depende del chip concreto, la precisión, el contexto y los kernels de CUDA o Metal; la capacidad no permite declarar un ganador universal.

## Reglas prácticas (marcadas como tales)

Son **reglas prácticas** para un instruct dense, 2026, no una calculadora:

- Pesos FP16 ≈ 2 bytes × parámetros. Un 8B son ~16 GB de pesos. KV y runtime van aparte.
- Q4_K_M suele caer cerca de medio byte a ~0,6 bytes por parámetro *en disco* — mira `ls -lh`, no el folklore.
- Si cabe en VRAM a tu contexto real, quédate. Si no: cuantiza, luego reduce contexto, luego descarga capas, luego un modelo más chico.
- Memoria unificada: deja margen para macOS y el navegador. Un Mac de "32 GB" no es una GPU de 32 GB.
- **MoE: la memoria sigue a los parámetros *totales*, no a los *activos***. No dimensionas VRAM con el "active 3B" de marketing. Un A3B 48B en Q4 son ~24-28 GB. Punto.
- KV a 128k de contexto en un dense 8B GQA: ~8-16 GB. Súmalo a los pesos antes de comprar hardware.

<TryThis>
Elige el modelo que de verdad quieres correr. Escribe: bytes de pesos (tamaño de archivo) + "KV desconocida." Cárgalo, pon contexto 4k y luego 32k si arranca, y mira la memoria residente. El delta es tu KV (más extras). Esa medida gana a cualquier blog. Repite con dos prompts concurrentes para ver la KV multiplicarse.
</TryThis>

## Caso — 128k que cabía en los pesos

Los pesos cabían en 24GB. A 64k de contexto el proceso murió. La KV era el segundo inquilino. Presupuesta ambos, o el número de contexto de la ficha es un reto, no una promesa.

<VisualSlot id="memory-hardware:2" caption="Tres vistas del tema: mapa, mecanismo y trade-off — interactivas y en español." />

## Fuentes

- Pope, R., et al. (2023). *Efficiently Scaling Transformer Inference*. https://arxiv.org/abs/2211.05102
- Kwon, W., et al. (2023). *PagedAttention* (vLLM). https://arxiv.org/abs/2309.06180
- NVIDIA H100 datasheet — HBM3 bandwidth and capacity. https://www.nvidia.com/en-us/data-center/h100/
- Apple Silicon — *Unified Memory architecture*. https://developer.apple.com/documentation/metal
- Fedus, W., Zoph, B., & Shazeer, N. (2022). *Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity* — MoE memory model. https://arxiv.org/abs/2101.03961

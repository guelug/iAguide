## Tokens are not words

A <Term id="token">token</Term> is the atom the model actually sees. English often lands near 0.7–1 word per token; code and other languages wander. You do not need a precise constant. You need the habit: **count tokens, not characters**, when you think about cost, context, and speed.

Tokenizers are part of the model artefact. A GGUF or a Hugging Face folder that ships the wrong tokenizer is a different model, even if the weights look familiar.

<Callout>
Rule of thumb, not a law: a page of English prose is often a few hundred tokens; a fat stack trace or a dumped JSON blob can burn thousands without looking “long” to your eye.
</Callout>

## Context is a window, not a filing cabinet

The <Term id="context-window">context window</Term> is how many tokens the model can attend over **right now**: system prompt, tools, the thread so far, and the tokens it is about to write. It is not long-term memory. When the window fills, something has to go — you compact, retrieve, or start a new thread.

Bigger windows are not free. They cost <Term id="kv-cache">KV cache</Term> memory and usually slow <Term id="prefill">prefill</Term>.

## Embeddings, briefly

An <Term id="embedding">embedding</Term> is a list of numbers. Nearby lists tend to mean related things. Token embeddings are the model's first look at each token; later layers mix them. Passage embeddings are what most “vector search” stacks store. You do not need the geometry. You need: **similarity is a score, not a proof**.

## Next token, forever

<Term id="next-token">Next-token prediction</Term> is the job: given the sequence so far, produce <Term id="logits">logits</Term> — a score per vocabulary item — then turn those into probabilities (see softmax in the maths module) and pick.

That is all generation is. Planning, tools, “reasoning” traces: extra tokens, extra loops, extra training. The model still emits one token at a time.

## Sampling knobs

- **<Term id="greedy">Greedy</Term>** (temperature 0, or argmax): always the top score. Repeatable if the runtime is deterministic.
- **<Term id="temperature">Temperature</Term>**: low sharpens the distribution; high flattens it. Very high becomes babble.
- **<Term id="top-p">Top-p</Term>**: keep the smallest set of tokens whose mass sums to p, then sample.
- **<Term id="top-k">Top-k</Term>**: keep only k tokens, then sample.

<Myth>
“Temperature 0 means the model is truthful.” No. It means the model is less random. A confident wrong token stays wrong.
</Myth>

## Prefill vs decode

<VisualSlot caption="Left: prefill writes a block of KV. Right: decode appends one token at a time." />

Two phases, one request:

1. **Prefill** — ingest the prompt (often in parallel). Build the KV cache. This is usually compute-heavy.
2. **Decode** — emit one token, append its keys/values, repeat. This is often **memory-bandwidth** heavy: you stream weights for a tiny amount of new work.

That is why a long system prompt hurts the first token, and why a long *generation* hurts tokens-per-second as the cache grows.

<TryThis>
In any local UI that shows token counts (llama.cpp server, Ollama, LM Studio), send the same short question twice: once alone, once after pasting a large file. Watch time-to-first-token vs tokens/second. You are looking at prefill vs decode, not at “the model getting confused.”
</TryThis>

## Determinism

Greedy + a deterministic kernel + a fixed prompt can replay. Sampling will not. Some GPU kernels are not bit-stable across runs. If you need replay — eval, tests — pin seed, temperature 0, and still treat GPU drift as possible.

## Chat models are the engine inside an agent

Hugging Face’s agents course puts it bluntly: the LLM is the **brain** of the agent, not the agent. Decoder-only transformers (Llama, Gemma, SmolLM, GPT-class models) predict the next token. Encoder models (BERT-class) emit embeddings. Encoder–decoder (T5, BART) map a sequence to a sequence. Agents almost always sit on a **chat / instruct** decoder: they consume a templated conversation and emit more tokens — including the tokens that mean “call this tool.”

That is the same next-token job as the rest of this page. Agency is extra loop, extra tools, extra training. The engine is unchanged.

Special tokens (`<|im_end|>`, `<|eot_id|>`, `<end_of_turn>`) are how a model knows a turn ended. They belong with the tokenizer; we unpack them in [tokenization](/en/m/tokenization).

This lesson rewrites ideas from the [Hugging Face Agents Course](https://huggingface.co/learn/agents-course) (Burtenshaw, Thomas, Simonini, Paniego), Apache-2.0.

<TryThis>
Take a weather question. Write (1) the tokens you think a chat model must emit to *call* `get_weather`, and (2) the tokens it emits *after* the harness appends “15°C, cloud.” You are looking at the engine inside the loop, not at a second brain.
</TryThis>

## Case — Alfred without a loop

A student wired HuggingChat to a coffee machine API and called it an agent. The model wrote lovely plans (“go to the kitchen…”) and never emitted a tool call, because nothing in the prompt named a function and nothing in the runtime parsed one. They had an engine. They did not have a harness. The loop lives in [the harness](/en/m/the-harness) and the dummy encoding in [dummy agent](/en/m/dummy-agent).

## Sources

- Hugging Face Agents Course — *The language of models*. https://huggingface.co/learn/agents-course (Burtenshaw, Thomas, Simonini, Paniego), Apache-2.0.
- Hugging Face NLP Course — *Tokenizers*. https://huggingface.co/learn/nlp-course

## Quiz

<Quiz
 questions={[
  {
    id: "q1",
    stem: "How many tokens does English text usually cost per word under a modern BPE tokenizer?",
    options: [
      { id: "a", text: "Exact: 1 token = 1 word" },
      { id: "b", text: "Roughly 0.7–1 token per word; code, URLs, and non-Latin scripts deviate a lot" },
      { id: "c", text: "10 tokens per word on average" },
      { id: "d", text: "A fixed number independent of the language" }
    ],
    correct: "b",
    explanation: "BPE tokenizers learn to chunk by frequency. English sits around 0.7–1 token per word. Code, URLs, and non-Latin scripts spend more tokens per the same amount of characters. Count tokens, not characters, when you think about cost and context."
  },
  {
    id: "q2",
    stem: "What three transformer architectures exist, and what is each for?",
    options: [
      { id: "a", text: "Encoder, decoder, encoder-decoder: the encoder emits embeddings (BERT); the decoder predicts the next token (Llama, GPT); the encoder-decoder maps sequence to sequence (T5, BART)" },
      { id: "b", text: "Only the decoder exists; the others are cancelled projects" },
      { id: "c", text: "Encoder is for vision, decoder is for text, encoder-decoder is unused" },
      { id: "d", text: "All three are synonyms, there is no difference" }
    ],
    correct: "a",
    explanation: "Hugging Face's course splits: encoder (BERT-class) for embeddings; decoder-only (Llama, Gemma, GPT) for autoregressive generation; encoder-decoder (T5, BART) for sequence-to-sequence. Agents typically rely on a chat/instruct decoder because it consumes conversational templates and emits the tool-call tokens."
  },
  {
    id: "q3",
    stem: "Why can a page of prose cost thousands of tokens even when it does not 'look long'?",
    options: [
      { id: "a", text: "Because tokens are Unicode characters" },
      { id: "b", text: "Because a JSON dump, stack trace, or long log chunks into many tokens per word; a visually short page can hide a high token density" },
      { id: "c", text: "Because browsers add hidden tokens" },
      { id: "d", text: "Because models charge a flat per-page fee" }
    ],
    correct: "b",
    explanation: "Cost is per million tokens, not per character. A stack trace, a formatted JSON, or a 200-line log can burn thousands of tokens without looking long. Rule of thumb: measure with the model's tokenizer (or an equivalent `tiktoken`), not a word counter."
  }
 ]}
/>

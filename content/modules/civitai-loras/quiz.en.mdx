<Quiz
 questions={[
  {
    id: "q1",
    stem: "Where does the Load LoRA node sit in a ComfyUI graph?",
    options: [
      { id: "a", text: "Before Load Checkpoint, as a model pre-processor" },
      { id: "b", text: "Replacing Load Checkpoint, because a LoRA is a small checkpoint" },
      { id: "c", text: "Between Load Checkpoint and KSampler, on MODEL and CLIP outputs, while CLIP Text Encode still wires to the CLIP that Load LoRA rewrote" },
      { id: "d", text: "After Save Image, because it is a post-processor" }
    ],
    correct: "c",
    explanation: "Comfy-Org's graph is Load Checkpoint → Load LoRA (on MODEL and CLIP) → CLIP Text Encode (CLIP is rewritten by Load LoRA before it reaches the encode) → KSampler → VAE Decode → Save Image. Load LoRA does not replace the checkpoint: it applies trainable low-rank matrices on top of a frozen base (Hu et al., 2022)."
  },
  {
    id: "q2",
    stem: "Why does an SD 1.5 LoRA produce noise on SDXL?",
    options: [
      { id: "a", text: "Because SDXL is bigger and needs more VRAM" },
      { id: "b", text: "Because the LoRA was trained for a specific weight shape and CLIP space; adding it to another architecture pushes the denoise trajectory off the manifold the VAE knows how to decode" },
      { id: "c", text: "Because you must restart ComfyUI before using a new LoRA" },
      { id: "d", text: "Because SDXL always needs a different VAE than the LoRA's" }
    ],
    correct: "b",
    explanation: "Load LoRA does not type-check families: it sums low-rank deltas into `model` and `clip`. Wrong shape or wrong CLIP means the latent trajectory leaves the manifold the VAE can decode. The CivitAI card names the base; match that base, or run the LoRA on an SD 1.5 like dreamshaper_8."
  },
  {
    id: "q3",
    stem: "What does `strength_model` mean, per Comfy-Org?",
    options: [
      { id: "a", text: "It is the resolution the LoRA should run at" },
      { id: "b", text: "It is the number of denoise steps applied to the adapter" },
      { id: "c", text: "It is a scalar that multiplies the delta Load LoRA adds to `MODEL`; separately `strength_clip` scales the delta on `CLIP`. The card names the recommended range" },
      { id: "d", text: "It is the final image quality in bits per pixel" }
    ],
    correct: "c",
    explanation: "Comfy-Org lists two independent inputs: `strength_model` for the deltas on the UNet/DiT, `strength_clip` for the deltas on the text encoder. Both default to 1.0. If the card says 0.6–0.8, going above 1.0 only works when you know the adapter tolerates it; outside the range the style breaks."
  }
 ]}
/>

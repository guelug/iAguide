<Quiz
 questions={[
  {
    id: "q1",
    stem: "Why does GRPO fit on one GPU per Unsloth's RL guide?",
    options: [
      { id: "a", text: "Because GRPO does not use PyTorch" },
      { id: "b", text: "Because GRPO samples G answers, scores them with a verifiable reward, and builds advantages with Z-score; there is no value model and no learned reward model in the PPO sense" },
      { id: "c", text: "Because GRPO runs on CPU" },
      { id: "d", text: "Because GRPO trains with 1-bit quantisation" }
    ],
    correct: "b",
    explanation: "GRPO (Group Relative Policy Optimization) drops the value model from classic PPO. Advantages come from Z-score within the group of G samples, so the per-step overhead is negligible compared to the model's own forward. RLVR = a **verifiable** reward (math, code, a small-checks rubric). If the reward is not checkable, GRPO does not save you."
  },
  {
    id: "q2",
    stem: "Why does Unsloth recommend starting GRPO from an Instruct checkpoint, not Base?",
    options: [
      { id: "a", text: "Because Instruct checkpoints are smaller" },
      { id: "b", text: "Because if the probability of a correct answer is ~0 under the initial model, GRPO never sees a good rollout and the baseline stays at reward 0 with no signal" },
      { id: "c", text: "Because Base does not fit in 24GB" },
      { id: "d", text: "Because Instruct checkpoints ship with the chat template" }
    ],
    correct: "b",
    explanation: "GRPO needs **some** good rollouts to build a distribution with variance. Starting from Base means almost every rollout scores 0; the Z-score of a uniform group is noise. Starting from Instruct gives a distribution already biased toward the expected format and grammar."
  },
  {
    id: "q3",
    stem: "Which six cells of the Kaggle Qwen3.8 conversational notebook should you change for your own dataset?",
    options: [
      { id: "a", text: "The six environment-setup cells" },
      { id: "b", text: "(1) dataset path / `load_dataset`, (2) the cell that renders `text` with the Qwen chat template, (3) `finetune_vision_layers`, (4) `max_seq_length`, (5) `max_steps` or epochs, (6) `output_dir` and the export cell" },
      { id: "c", text: "Only the first cell" },
      { id: "d", text: "Only the last save cell" }
    ],
    correct: "b",
    explanation: "The [Kaggle conversational notebook](https://github.com/unslothai/notebooks/blob/main/nb/Kaggle-Qwen3.8_(27B)-Conversational.ipynb) has six cells a real user changes for their JSONL and `max_seq_length=2048`. If you cannot name six cells, you do not have a dataset — you have a vibe."
  }
 ]}
/>

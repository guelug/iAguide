<Quiz
 questions={[
  {
    id: "q1",
    stem: "Which of Hugging Face's four inference products actually downloads the weights to the client?",
    options: [
      { id: "a", text: "Inference Providers" },
      { id: "b", text: "Inference Endpoints" },
      { id: "c", text: "Local Transformers via `pipeline` or `AutoModel.from_pretrained`: the shards live in the Hub cache and run on your GPU (or CPU)" },
      { id: "d", text: "None, all are serverless" }
    ],
    correct: "c",
    explanation: "Local Transformers is the only one of the four that puts the weights on your box. Inference Providers is serverless, Endpoints is dedicated hardware Hugging Face operates, and Jobs runs a Docker script on Hub GPU/TPU. The partner catalog shifts; who runs on your GPU is up to you, via `device_map`."
  },
  {
    id: "q2",
    stem: "What does the `:fastest` suffix do on an Inference Providers call?",
    options: [
      { id: "a", text: "It forces the cheapest partner regardless of latency" },
      { id: "b", text: "It tells the router to pick the partner with the lowest measured latency for that model at that moment; it does not guarantee an SLA" },
      { id: "c", text: "It reserves a dedicated GPU for 24 hours" },
      { id: "d", text: "It skips the queue and goes straight to the model" }
    ],
    correct: "b",
    explanation: "The `:fastest` suffix is a hint to the router, not a promise: the partner the router picks varies by measured latency. For batch or reproducible loads, pin a specific provider instead of the router. The Inference Providers page describes it as an OpenAI-shaped unified API."
  },
  {
    id: "q3",
    stem: "How does `hf jobs run` differ from an Inference Providers call?",
    options: [
      { id: "a", text: "`hf jobs run` executes a Docker script on Hub GPUs (compute by hours), not a chat round-trip; Inference Providers is a serverless chat/embeddings/vision API" },
      { id: "b", text: "They are the same thing with a different name" },
      { id: "c", text: "`hf jobs run` only runs on CPU; Inference Providers only on GPU" },
      { id: "d", text: "`hf jobs run` requires an Enterprise plan" }
    ],
    correct: "a",
    explanation: "`hf jobs run` launches a script (a Docker script) on Hub hardware for batch jobs: fine-tuning, eval, a bulk download. Inference Providers is the OpenAI-shaped unified API for chat, embeddings, vision, and friends. The CLI's docs document both as separate products."
  }
 ]}
/>

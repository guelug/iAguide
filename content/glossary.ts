import type { Locale } from "@/i18n/routing";

export type GlossaryTerm = {
  id: string;
  module: string;
  term: { en: string; es: string };
  def: { en: string; es: string };
};

export const GLOSSARY: GlossaryTerm[] = [
  { id: "model", module: "orientation", term: { en: "model", es: "modelo" }, def: { en: "A file of learned weights plus a recipe for scoring the next token. Not a hand-written program of rules.", es: "Un archivo de pesos aprendidos más una receta para puntuar el siguiente token. No es un programa de reglas escritas a mano." } },
  { id: "weights", module: "orientation", term: { en: "weights", es: "pesos" }, def: { en: "The numbers that are the model. They live on disk. Changing them is training or an adapter, not a longer prompt.", es: "Los números que son el modelo. Viven en disco. Cambiarlos es entrenar o poner un adapter, no alargar el prompt." } },
  { id: "parameter", module: "orientation", term: { en: "parameter", es: "parámetro" }, def: { en: "One learned number in the file. «7B» means about seven billion parameters: size, not a grade.", es: "Un número aprendido del archivo. «7B» son unos siete mil millones de parámetros: tamaño, no una nota." } },
  { id: "hallucination", module: "orientation", term: { en: "hallucination", es: "alucinación" }, def: { en: "Fluent next-token continuation that is not grounded in a retrieved fact. Confidence is not evidence.", es: "Una continuación fluida del siguiente token que no está anclada a un hecho pegado en el prompt. La seguridad no es prueba." } },
  { id: "token", module: "language-of-models", term: { en: "token", es: "token" }, def: { en: "A chunk of text the model reads and writes. Not always a word: hello may be one token; a rare name may be several.", es: "Un trozo de texto que el modelo lee y escribe. No siempre es una palabra." } },
  { id: "tokenizer", module: "language-of-models", term: { en: "tokenizer", es: "tokenizador" }, def: { en: "The piece that splits text into token ids. It is part of the model artifact: swap it and you have another model.", es: "La pieza que parte el texto en ids. Viaja con el modelo: cámbiala y tienes otro modelo." } },
  { id: "context-window", module: "language-of-models", term: { en: "context window", es: "ventana de contexto" }, def: { en: "The maximum number of tokens the model can attend over in one pass — prompt plus generation so far.", es: "El número máximo de tokens sobre los que el modelo puede atender en una pasada: prompt más lo generado." } },
  { id: "embedding", module: "language-of-models", term: { en: "embedding", es: "embedding" }, def: { en: "A list of numbers that places a token (or a passage) in a space where nearby points mean related things.", es: "Una lista de números que sitúa un token (o un pasaje) en un espacio donde los puntos cercanos significan cosas afines." } },
  { id: "next-token", module: "language-of-models", term: { en: "next-token prediction", es: "predicción del siguiente token" }, def: { en: "The core job: given tokens so far, score every token in the vocabulary as the next one.", es: "La tarea central: dados los tokens hasta ahora, puntuar cada token del vocabulario como el siguiente." } },
  { id: "logits", module: "language-of-models", term: { en: "logits", es: "logits" }, def: { en: "Raw scores for each vocabulary item, before they are turned into probabilities.", es: "Puntuaciones crudas para cada ítem del vocabulario, antes de convertirse en probabilidades." } },
  { id: "softmax", module: "maths-you-need", term: { en: "softmax", es: "softmax" }, def: { en: "Turns a vector of scores into a probability distribution that sums to 1.", es: "Convierte un vector de puntuaciones en una distribución de probabilidad que suma 1." } },
  { id: "sampling", module: "language-of-models", term: { en: "sampling", es: "muestreo" }, def: { en: "Drawing the next token from the probability distribution instead of always taking the top score.", es: "Elegir el siguiente token de la distribución en lugar de tomar siempre la puntuación más alta." } },
  { id: "temperature", module: "language-of-models", term: { en: "temperature", es: "temperatura" }, def: { en: "A knob that sharpens (low) or flattens (high) the next-token distribution. Zero is greedy.", es: "Un control que afila (baja) o aplana (alta) la distribución del siguiente token. Cero es greedy." } },
  { id: "top-p", module: "language-of-models", term: { en: "top-p (nucleus)", es: "top-p (núcleo)" }, def: { en: "Sample only from the smallest set of tokens whose probabilities sum to p.", es: "Muestrear solo del conjunto más pequeño de tokens cuyas probabilidades suman p." } },
  { id: "top-k", module: "language-of-models", term: { en: "top-k", es: "top-k" }, def: { en: "Sample only from the k highest-probability tokens.", es: "Muestrear solo de los k tokens con mayor probabilidad." } },
  { id: "prefill", module: "language-of-models", term: { en: "prefill", es: "prefill" }, def: { en: "The prompt-processing pass: prompt tokens are ingested, usually in parallel, and the KV cache is filled.", es: "La pasada que procesa el prompt: se ingieren los tokens, casi siempre en paralelo, y se llena la caché KV." } },
  { id: "decode", module: "language-of-models", term: { en: "decode", es: "decode" }, def: { en: "The generation pass: one new token at a time, reusing the KV cache.", es: "La pasada de generación: un token nuevo cada vez, reutilizando la caché KV." } },
  { id: "kv-cache", module: "language-of-models", term: { en: "KV cache", es: "caché KV" }, def: { en: "Stored key and value tensors from attention for tokens already seen, so decode does not recompute them.", es: "Tensores de keys y values de atención de tokens ya vistos, para no recomputarlos en el decode." } },
  { id: "greedy", module: "language-of-models", term: { en: "greedy decoding", es: "decodificación greedy" }, def: { en: "Always pick the highest-probability next token. Deterministic for a fixed prompt on a deterministic runtime.", es: "Elegir siempre el token de mayor probabilidad. Determinista con prompt y runtime fijos." } },
  { id: "harness", module: "the-harness", term: { en: "harness", es: "arnés" }, def: { en: "The loop around a model: system prompt, tools, threads, permissions, memory, and control flow. Not the model weights.", es: "El bucle alrededor de un modelo: system prompt, herramientas, hilos, permisos, memoria y control de flujo. No son los pesos." } },
  { id: "system-prompt", module: "the-harness", term: { en: "system prompt / profile", es: "system prompt / perfil" }, def: { en: "The standing instructions injected every turn (or cached) that shape role, style, and tool policy.", es: "Las instrucciones permanentes que se inyectan (o se cachean) y marcan rol, estilo y política de herramientas." } },
  { id: "tool", module: "the-harness", term: { en: "tool", es: "herramienta" }, def: { en: "A function the model can request: name, JSON schema, and an implementation the harness runs.", es: "Una función que el modelo puede pedir: nombre, esquema JSON e implementación que ejecuta el arnés." } },
  { id: "thread", module: "the-harness", term: { en: "thread", es: "hilo" }, def: { en: "A durable conversation: messages, tool results, and often a working directory or memory store.", es: "Una conversación durable: mensajes, resultados de herramientas y a menudo un directorio de trabajo o memoria." } },
  { id: "subagent", module: "the-harness", term: { en: "subagent", es: "subagente" }, def: { en: "A nested harness run with its own prompt, tools, and usually a smaller context, reporting back a summary.", es: "Una ejecución anidada del arnés con su propio prompt, herramientas y, casi siempre, un contexto más pequeño." } },
  { id: "permissions", module: "the-harness", term: { en: "permissions", es: "permisos" }, def: { en: "What the harness will actually execute: which tools, which paths, whether network is allowed.", es: "Lo que el arnés ejecutará de verdad: qué herramientas, qué rutas, si hay red." } },
  { id: "prompt-cache", module: "the-harness", term: { en: "prompt cache / prefix cache", es: "caché de prompt / prefijo" }, def: { en: "Reuse of computed prompt prefixes across requests. Not the same as the KV cache of one request.", es: "Reutilizar prefijos de prompt ya computados entre peticiones. No es la caché KV de una sola petición." } },
  { id: "compaction", module: "the-harness", term: { en: "compaction", es: "compactación" }, def: { en: "Summarising or dropping old thread turns so the next call fits in the context window.", es: "Resumir o tirar turnos viejos del hilo para que la siguiente llamada quepa en la ventana de contexto." } },
  { id: "kernel", module: "the-harness", term: { en: "kernel", es: "kernel" }, def: { en: "Two common meanings: a GPU kernel (the compiled GPU program) and a runtime kernel (the inner loop of a harness).", es: "Dos sentidos habituales: un kernel de GPU (el programa compilado) y el kernel de un runtime (el bucle interno del arnés)." } },
  { id: "nous-hermes", module: "the-harness", term: { en: "Nous Hermes", es: "Nous Hermes" }, def: { en: "A family of open-weight chat models from Nous Research. A model, not a harness. The harness is the loop you put around any model.", es: "Una familia de modelos chat de pesos abiertos de Nous Research. Es un modelo, no un arnés. El arnés es el bucle que pones alrededor de cualquier modelo." } },
  { id: "messages-api", module: "calls-cache-threads", term: { en: "messages API", es: "API de messages" }, def: { en: "Chat-style request: a list of role/content messages, often with tool calls in-band.", es: "Petición estilo chat: una lista de mensajes rol/contenido, a menudo con tool calls incluidos." } },
  { id: "completions-api", module: "calls-cache-threads", term: { en: "completions API", es: "API de completions" }, def: { en: "Raw prefix-completion: you send text, the model continues it. Still used under the hood and in some local servers.", es: "Completar un prefijo: envías texto y el modelo lo continúa. Sigue usándose por debajo y en algunos servidores locales." } },
  { id: "tool-call", module: "calls-cache-threads", term: { en: "tool_call", es: "tool_call" }, def: { en: "A structured request from the model to run a tool: id, name, arguments. The harness executes and returns a tool result message.", es: "Una petición estructurada del modelo para ejecutar una herramienta: id, nombre, argumentos." } },
  { id: "streaming", module: "calls-cache-threads", term: { en: "streaming", es: "streaming" }, def: { en: "Tokens (and sometimes tool-call fragments) arrive as they are generated, instead of waiting for the full response.", es: "Los tokens (y a veces fragmentos de tool_call) llegan mientras se generan." } },
  { id: "http-cache", module: "calls-cache-threads", term: { en: "HTTP cache", es: "caché HTTP" }, def: { en: "Ordinary web caching of GET responses. Almost never what people mean by the model cache.", es: "La caché web de respuestas GET. Casi nunca es lo que se quiere decir con la caché del modelo." } },
  { id: "turn", module: "calls-cache-threads", term: { en: "turn", es: "turno" }, def: { en: "One user message plus the model's work until it stops or returns control — may include several tool rounds.", es: "Un mensaje de usuario más el trabajo del modelo hasta que para — puede incluir varias rondas de herramientas." } },
  { id: "session", module: "calls-cache-threads", term: { en: "session", es: "sesión" }, def: { en: "A looser unit than a thread: often the lifetime of a tab, a login, or a process. Do not assume it maps 1:1 to context.", es: "Una unidad más laxa que un hilo: a menudo la vida de una pestaña o un proceso. No asumas que coincide con el contexto." } },
  { id: "closed-model", module: "model-zoo", term: { en: "closed model", es: "modelo cerrado" }, def: { en: "You call an API; you do not get the weights. The vendor chooses hardware, quant, and often the system prompt.", es: "Llamas a una API; no recibes los pesos. El proveedor elige hardware, cuantización y a menudo el system prompt." } },
  { id: "open-weights", module: "model-zoo", term: { en: "open weights", es: "pesos abiertos" }, def: { en: "The parameters are downloadable. The training data, code, and license may still be restricted.", es: "Los parámetros se pueden descargar. Los datos de entrenamiento, el código y la licencia pueden seguir restringidos." } },
  { id: "open-source-model", module: "model-zoo", term: { en: "open source (model)", es: "código abierto (modelo)" }, def: { en: "A stronger claim than open weights: look at the license, data, and training code. Many open models are open weights only.", es: "Una afirmación más fuerte que pesos abiertos: mira licencia, datos y código de entrenamiento." } },
  { id: "dense", module: "model-zoo", term: { en: "dense model", es: "modelo dense" }, def: { en: "Every parameter is used on every token. Contrast with Mixture of Experts.", es: "Cada parámetro se usa en cada token. Contrasta con Mixture of Experts." } },
  { id: "moe", module: "model-zoo", term: { en: "Mixture of Experts (MoE)", es: "Mixture of Experts (MoE)" }, def: { en: "A layer routes each token to a few expert MLPs. Total parameters can be huge; active parameters per token are smaller.", es: "Una capa enruta cada token a unos pocos expertos. Los parámetros totales pueden ser enormes; los activos por token son menos." } },
  { id: "base-model", module: "model-zoo", term: { en: "base model", es: "modelo base" }, def: { en: "Pretrained on next-token prediction, not tuned to follow instructions. Completes text; does not chat by default.", es: "Preentrenado para predecir el siguiente token, no ajustado a seguir instrucciones." } },
  { id: "instruct", module: "model-zoo", term: { en: "instruct / chat model", es: "modelo instruct / chat" }, def: { en: "A base model after SFT (and often preference training) so it follows a chat template.", es: "Un modelo base tras SFT (y a menudo preferencias) para seguir una plantilla de chat." } },
  { id: "reasoning-model", module: "model-zoo", term: { en: "reasoning model", es: "modelo de razonamiento" }, def: { en: "A variant trained or prompted to spend tokens on intermediate work before the answer. Costs more tokens; not magic.", es: "Una variante entrenada o inducida a gastar tokens en trabajo intermedio antes de la respuesta." } },
  { id: "model-card", module: "model-zoo", term: { en: "model card", es: "ficha del modelo" }, def: { en: "The Hugging Face (or vendor) page: license, architecture, context length, chat template, intended use, evals.", es: "La página en Hugging Face o del proveedor: licencia, arquitectura, contexto, plantilla de chat, uso previsto, evaluaciones." } },
  { id: "pretrain", module: "training", term: { en: "pretraining", es: "preentrenamiento" }, def: { en: "The expensive next-token run on a huge corpus that produces a base model. Not a laptop job.", es: "La pasada cara de siguiente-token sobre un corpus enorme que produce un modelo base. No es trabajo de portátil." } },
  { id: "sft", module: "training", term: { en: "SFT", es: "SFT" }, def: { en: "Supervised fine-tuning: train on prompt and desired completion pairs so the model imitates that style.", es: "Ajuste supervisado: entrenar con pares de prompt y continuación deseada para que el modelo imite ese estilo." } },
  { id: "rlhf", module: "training", term: { en: "RLHF", es: "RLHF" }, def: { en: "Reinforcement learning from human feedback: a reward model trained on human preferences, then a policy update (classically PPO).", es: "Aprendizaje por refuerzo con feedback humano: un modelo de recompensa con preferencias humanas y luego una actualización de política (clásicamente PPO)." } },
  { id: "dpo", module: "training", term: { en: "DPO", es: "DPO" }, def: { en: "Direct Preference Optimization: skip a separate reward model; use preference pairs to move the policy directly.", es: "Direct Preference Optimization: sin modelo de recompensa aparte; usa pares de preferencia para mover la política." } },
  { id: "rlaif", module: "training", term: { en: "RLAIF", es: "RLAIF" }, def: { en: "Like RLHF, but preference labels or critiques come from an AI judge rather than only humans.", es: "Como RLHF, pero las etiquetas de preferencia vienen de un juez de IA." } },
  { id: "ppo", module: "training", term: { en: "PPO", es: "PPO" }, def: { en: "Proximal Policy Optimization: an RL algorithm that updates the policy in small, clipped steps. Used in classic RLHF.", es: "Proximal Policy Optimization: un algoritmo de RL que actualiza la política a pasos recortados. Usado en el RLHF clásico." } },
  { id: "grpo", module: "training", term: { en: "GRPO", es: "GRPO" }, def: { en: "Group Relative Policy Optimization: a critic-light RL style used for reasoning and tool traces — sample a group of rollouts and learn from relative scores.", es: "Group Relative Policy Optimization: un estilo de RL sin crítico pesado, usado en trazas de razonamiento y herramientas." } },
  { id: "lora", module: "fine-tuning", term: { en: "LoRA", es: "LoRA" }, def: { en: "Low-Rank Adaptation: freeze the base weights; learn a pair of small matrices whose product is the update.", es: "Low-Rank Adaptation: congela los pesos base y aprende un par de matrices pequeñas cuyo producto es la actualización." } },
  { id: "qlora", module: "fine-tuning", term: { en: "QLoRA", es: "QLoRA" }, def: { en: "LoRA while the base model sits in 4-bit (typically NF4) in memory. Makes larger models fit on one GPU for training.", es: "LoRA con el modelo base en 4 bits (típicamente NF4) en memoria. Hace que modelos más grandes quepan en una GPU para entrenar." } },
  { id: "full-ft", module: "fine-tuning", term: { en: "full fine-tune", es: "ajuste completo" }, def: { en: "Update all or most parameters. Highest capacity, highest memory and overfit risk.", es: "Actualizar todos o casi todos los parámetros. Más capacidad, más memoria y más riesgo de sobreajuste." } },
  { id: "peft", module: "fine-tuning", term: { en: "PEFT", es: "PEFT" }, def: { en: "Parameter-efficient fine-tuning: the family that includes LoRA, adapters, prefix-tuning.", es: "Ajuste eficiente en parámetros: la familia que incluye LoRA, adapters, prefix-tuning." } },
  { id: "gguf", module: "local-inference", term: { en: "GGUF", es: "GGUF" }, def: { en: "The llama.cpp file format: weights, tokenizer, and metadata in one file. Usually already quantized.", es: "El formato de llama.cpp: pesos, tokenizador y metadatos en un archivo. Suele venir ya cuantizado." } },
  { id: "safetensors", module: "local-inference", term: { en: "safetensors", es: "safetensors" }, def: { en: "A safe tensor container used by Hugging Face and many GPU servers. Not itself a quant scheme.", es: "Un contenedor de tensores usado por Hugging Face y muchos servidores GPU. No es un esquema de cuantización." } },
  { id: "exl2", module: "local-inference", term: { en: "EXL2", es: "EXL2" }, def: { en: "ExLlamaV2 GPU-oriented quant format with a target bitrate. Not interchangeable with GGUF.", es: "Formato de cuantización de ExLlamaV2 orientado a GPU, con un bitrate objetivo. No es intercambiable con GGUF." } },
  { id: "llamacpp", module: "local-inference", term: { en: "llama.cpp", es: "llama.cpp" }, def: { en: "C/C++ runtime for GGUF models. CPU, Metal, CUDA, and more. The engine under Ollama and many desktop apps.", es: "Runtime C/C++ para modelos GGUF. CPU, Metal, CUDA y más. El motor bajo Ollama y muchas apps de escritorio." } },
  { id: "ollama", module: "local-inference", term: { en: "Ollama", es: "Ollama" }, def: { en: "A local server and CLI that pulls GGUF-style models and exposes an HTTP API. Convenient, fewer knobs.", es: "Un servidor local y CLI que baja modelos estilo GGUF y expone una API HTTP. Cómodo, menos palancas." } },
  { id: "vllm", module: "local-inference", term: { en: "vLLM", es: "vLLM" }, def: { en: "A GPU inference server built for throughput: paged KV cache, continuous batching. Typical for NVIDIA boxes serving many users.", es: "Servidor de inferencia GPU pensado para throughput: KV cache paginada, batching continuo." } },
  { id: "mlx", module: "local-inference", term: { en: "MLX", es: "MLX" }, def: { en: "Apple array framework. MLX-LM runs and can fine-tune models in unified memory on Apple Silicon.", es: "El framework de arrays de Apple. MLX-LM ejecuta y puede ajustar modelos en la memoria unificada de Apple Silicon." } },
  { id: "quantization", module: "quantization", term: { en: "quantization", es: "cuantización" }, def: { en: "Storing and often computing with weights in fewer bits. Shrinks memory; can nick quality.", es: "Guardar y a menudo computar pesos con menos bits. Reduce memoria; puede rozar la calidad." } },
  { id: "q4km", module: "quantization", term: { en: "Q4_K_M", es: "Q4_K_M" }, def: { en: "A llama.cpp K-quant: roughly 4-bit, mixed precision, medium quality profile. A common default GGUF.", es: "Un K-quant de llama.cpp: unos 4 bits, precisión mixta, perfil medio. Un GGUF por defecto muy habitual." } },
  { id: "gptq", module: "quantization", term: { en: "GPTQ", es: "GPTQ" }, def: { en: "A post-training GPU quant method, often 4-bit, that uses calibration data. Common in vLLM stacks.", es: "Un método de cuantización GPU post-entrenamiento, a menudo 4 bits, con datos de calibración." } },
  { id: "awq", module: "quantization", term: { en: "AWQ", es: "AWQ" }, def: { en: "Activation-aware Weight Quantization: protects salient weights using activation statistics. Also common on GPU servers.", es: "Activation-aware Weight Quantization: protege pesos sensibles usando estadísticas de activación." } },
  { id: "nf4", module: "quantization", term: { en: "NF4", es: "NF4" }, def: { en: "4-bit NormalFloat, used by QLoRA and bitsandbytes. A training-time data type, not a GGUF filename.", es: "4-bit NormalFloat, usado por QLoRA y bitsandbytes. Un tipo en entrenamiento, no un nombre de archivo GGUF." } },
  { id: "vram", module: "memory-hardware", term: { en: "VRAM", es: "VRAM" }, def: { en: "Memory on a discrete GPU. Fast for the GPU; size is the usual limit for weights plus KV cache.", es: "Memoria de una GPU discreta. Rápida para la GPU; su tamaño suele limitar pesos más caché KV." } },
  { id: "unified-memory", module: "memory-hardware", term: { en: "unified memory", es: "memoria unificada" }, def: { en: "On Apple Silicon, CPU and GPU share one pool. No PCIe copy of the whole model; bandwidth is still finite.", es: "En Apple Silicon, CPU y GPU comparten un pool. No hay copia PCIe del modelo entero; el ancho de banda sigue siendo finito." } },
  { id: "pcie", module: "memory-hardware", term: { en: "PCIe", es: "PCIe" }, def: { en: "The bus between CPU RAM and a discrete GPU. Moving tensors across it is slower than staying in VRAM.", es: "El bus entre la RAM y una GPU discreta. Mover tensores por él es más lento que quedarse en VRAM." } },
  { id: "bandwidth", module: "memory-hardware", term: { en: "memory bandwidth", es: "ancho de banda de memoria" }, def: { en: "How fast you can stream weights and KV. Decode is often bandwidth-bound, not FLOPs-bound.", es: "Qué tan rápido puedes leer pesos y KV. El decode suele estar limitado por ancho de banda, no por FLOPs." } },
  { id: "attention", module: "maths-you-need", term: { en: "attention", es: "atención" }, def: { en: "For each token, mix information from other tokens using learned queries, keys, and values.", es: "Para cada token, mezclar información de otros tokens usando queries, keys y values aprendidos." } },
  { id: "cross-entropy", module: "maths-you-need", term: { en: "cross-entropy", es: "entropía cruzada" }, def: { en: "The usual training loss for next-token prediction: how surprised the model was by the true next token.", es: "La pérdida habitual al predecir el siguiente token: cuánto sorprendió al modelo el token verdadero." } },
  { id: "gradient", module: "maths-you-need", term: { en: "gradient", es: "gradiente" }, def: { en: "The direction to nudge parameters to reduce loss. Backprop computes it; optimizers step along it.", es: "La dirección para mover parámetros y reducir la pérdida. El backprop lo calcula; el optimizador da el paso." } },
  { id: "rank", module: "maths-you-need", term: { en: "rank (LoRA)", es: "rango (LoRA)" }, def: { en: "The inner dimension of the LoRA pair. Small rank means few extra parameters; too small may underfit.", es: "La dimensión interna del par LoRA. Rango pequeño significa pocos parámetros extra; demasiado pequeño puede quedarse corto." } },
  { id: "chat-template", module: "training", term: { en: "chat template", es: "plantilla de chat" }, def: { en: "The exact serialization of a turn — system, user, assistant — that a model was trained on. ChatML, Alpaca and Llama-3 are different ones. Fine-tune with the template you will infer with, or the model answers a format it never saw.", es: "La serialización exacta de un turno — system, user, assistant — con la que se entrenó un modelo. ChatML, Alpaca y Llama-3 son distintas. Afina con la plantilla que usarás al inferir, o el modelo responderá a un formato que nunca vio." } },
  { id: "scaling-laws", module: "training", term: { en: "scaling laws", es: "leyes de escala" }, def: { en: "The empirical curves relating compute, data and parameters to loss. They predict the long plateau: each doubling of compute removes a fixed percentage of loss, not a fixed amount.", es: "Las curvas empíricas que relacionan cómputo, datos y parámetros con la pérdida. Predicen la meseta larga: cada duplicación de cómputo quita un porcentaje fijo de pérdida, no una cantidad fija." } },
  { id: "chinchilla", module: "training", term: { en: "Chinchilla", es: "Chinchilla" }, def: { en: "Hoffmann et al. (2022) showed most models were undertrained: for a given parameter count, more tokens buy more quality than more parameters. It moved the field from bigger models to better-fed ones.", es: "Hoffmann et al. (2022) demostró que la mayoría de modelos estaban subentrenados: para un número dado de parámetros, más tokens compran más calidad que más parámetros. Movió el campo de modelos más grandes a modelos mejor alimentados." } },
  { id: "quality-filtering", module: "data", term: { en: "quality filtering", es: "filtrado de calidad" }, def: { en: "Dropping bad documents before training. Can be a rule as plain as words-per-sentence, or a classifier trained on human-marked text. Every filter carries the bias of whoever defined good.", es: "Descartar documentos malos antes de entrenar. Puede ser una regla tan simple como palabras por frase, o un clasificador entrenado sobre texto marcado por humanos. Todo filtro arrastra el sesgo de quien definió lo bueno." } },
  { id: "catastrophic-forgetting", module: "unsloth-qwen38", term: { en: "catastrophic forgetting", es: "olvido catastrófico" }, def: { en: "When fine-tuning on a narrow task overwrites a capability the base model had. SFT on short direct answers is the classic case: the model keeps completing, but stops showing the reasoning you paid for.", es: "Cuando afinar sobre una tarea estrecha sobrescribe una capacidad que el modelo base tenía. El SFT sobre respuestas directas cortas es el caso clásico: el modelo sigue completando, pero deja de mostrar el razonamiento que pagaste." } },
  { id: "neftune", module: "fine-tuning", term: { en: "NEFTune", es: "NEFTune" }, def: { en: "Adding uniform noise to input embeddings during training. It sounds like a mistake and it works: it improves robustness when you are fine-tuning on only hundreds of pairs.", es: "Añadir ruido uniforme a los embeddings de entrada durante el entrenamiento. Suena a error y funciona: mejora la robustez cuando afinas con solo cientos de pares." } },
  { id: "merge-and-unload", module: "fine-tuning", term: { en: "merge and unload", es: "merge and unload" }, def: { en: "Folding a LoRA adapter into the base weights and exporting full weights. You lose the ability to swap adapters and gain a single artifact to quantize or serve.", es: "Fusionar un adapter LoRA con los pesos base y exportar pesos completos. Pierdes la posibilidad de intercambiar adapters y ganas un único artefacto que cuantizar o servir." } },
  { id: "prompt-engineering", module: "fine-tuning", term: { en: "prompt engineering", es: "ingeniería de prompt" }, def: { en: "Changing the instructions instead of the weights. It is free infrastructure and iterates in minutes, so it is the thing to exhaust before a LoRA, which you pay for once and amortize.", es: "Cambiar las instrucciones en vez de los pesos. Es infraestructura gratis y se itera en minutos, así que es lo que hay que agotar antes de un LoRA, que pagas una vez y amortizas." } },
  { id: "few-shot-eval", module: "fine-tuning", term: { en: "few-shot eval", es: "eval de pocos ejemplos" }, def: { en: "Judging a model on a handful of examples. Tempting and unreliable: too few cases to capture variance. Hold out a real eval set and judge it consistently instead.", es: "Juzgar un modelo con un puñado de ejemplos. Tentador y poco fiable: son pocos casos para capturar la varianza. Reserva un set de eval de verdad y júzgalo de forma consistente." } },
  { id: "constitutional-ai", module: "reinforcement-learning", term: { en: "Constitutional AI", es: "Constitutional AI" }, def: { en: "A judge that scores against a written list of principles rather than raw preference. Useful when you need the judge's reasoning to be auditable instead of a number you must trust.", es: "Un juez que puntúa contra una lista escrita de principios en vez de preferencia cruda. Útil cuando necesitas que el razonamiento del juez sea auditable en vez de un número del que fiarte." } },
  { id: "ddp", module: "distributed-training", term: { en: "DDP (distributed data parallel)", es: "DDP (data parallel distribuido)" }, def: { en: "Every GPU holds the whole model and a different slice of the batch, then gradients are averaged. The bread and butter for SFT and LoRA while the model still fits on one card.", es: "Cada GPU guarda el modelo entero y una porción distinta del batch, y luego se promedian los gradientes. El pan de cada día para SFT y LoRA mientras el modelo quepa en una sola tarjeta." } },
  { id: "fsdp", module: "distributed-training", term: { en: "FSDP", es: "FSDP" }, def: { en: "PyTorch's native ZeRO-3: each GPU keeps only 1/N of the weights and gathers them on demand. More communication, but it trains models that would never fit replicated.", es: "El ZeRO-3 nativo de PyTorch: cada GPU guarda solo 1/N de los pesos y los reúne bajo demanda. Más comunicación, pero entrena modelos que replicados no cabrían nunca." } },
  { id: "zero", module: "distributed-training", term: { en: "ZeRO", es: "ZeRO" }, def: { en: "Zero Redundancy Optimizer: instead of replicating optimizer state on every GPU, shard it. Stages 1 to 3 shard progressively more — optimizer, then gradients, then parameters.", es: "Zero Redundancy Optimizer: en vez de replicar el estado del optimizador en cada GPU, se reparte. Las fases 1 a 3 reparten progresivamente más: optimizador, luego gradientes, luego parámetros." } },
  { id: "megatron-lm", module: "distributed-training", term: { en: "Megatron-LM", es: "Megatron-LM" }, def: { en: "The work that popularized tensor parallelism — splitting a single layer's matrices across GPUs. Still the pattern most frameworks use to parallelize large layers.", es: "El trabajo que popularizó el tensor parallel: partir las matrices de una sola capa entre GPUs. Sigue siendo el patrón que usan la mayoría de frameworks para paralelizar capas grandes." } },
  { id: "tensor-parallel", module: "attnres-optimize", term: { en: "tensor parallel", es: "tensor parallel" }, def: { en: "Splitting one layer's matrices across devices so a matmul runs in pieces. It already splits the sequence for the big matmuls, so the block cache should be split the same way.", es: "Partir las matrices de una capa entre dispositivos para que un matmul corra a trozos. Ya parte la secuencia para los matmuls grandes, así que la caché de bloques debería partirse igual." } },
  { id: "gpipe", module: "distributed-training", term: { en: "GPipe", es: "GPipe" }, def: { en: "A pipeline-parallel schedule: split layers into stages, push micro-batches through, flush at the end of each step. Simple, and it pays for that with a large idle bubble.", es: "Un planificador de pipeline parallel: parte las capas en etapas, empuja micro-batches y vacía al final de cada paso. Simple, y lo paga con una burbuja de inactividad grande." } },
  { id: "pipedream", module: "distributed-training", term: { en: "PipeDream", es: "PipeDream" }, def: { en: "A pipeline schedule that keeps stages busy by letting several micro-batches be in flight at once. Better utilization than GPipe, at the cost of holding more weight versions.", es: "Un planificador de pipeline que mantiene las etapas ocupadas dejando varios micro-batches en vuelo a la vez. Mejor utilización que GPipe, a costa de guardar más versiones de pesos." } },
  { id: "pipedream-flush", module: "distributed-training", term: { en: "1F1B (PipeDream-Flush)", es: "1F1B (PipeDream-Flush)" }, def: { en: "One forward, one backward: each stage alternates so memory stays bounded while the bubble stays small. The schedule most large-model training actually runs.", es: "Un forward, un backward: cada etapa alterna para que la memoria quede acotada y la burbuja siga siendo pequeña. El planificador que de verdad corre en la mayoría de entrenamientos grandes." } },
  { id: "nccl", module: "distributed-training", term: { en: "NCCL", es: "NCCL" }, def: { en: "NVIDIA's collective communication library, which PyTorch uses for DDP and FSDP. Tuned for NVLink and Infiniband; it treats plain Ethernet badly.", es: "La librería de comunicación colectiva de NVIDIA, que PyTorch usa para DDP y FSDP. Está afinada para NVLink e Infiniband; con Ethernet normal se porta mal." } },
  { id: "deepspeed-moe", module: "distributed-training", term: { en: "DeepSpeed-MoE", es: "DeepSpeed-MoE" }, def: { en: "A reference implementation of expert parallelism, where all-to-all communication becomes the dominant pattern and the interconnect turns into the real bottleneck.", es: "Una implementación de referencia del expert parallel, donde la comunicación all-to-all pasa a ser el patrón dominante y la red entre GPUs se convierte en el cuello de botella real." } },
  { id: "expert-offload", module: "mixture-of-experts", term: { en: "expert offloading", es: "expert offloading" }, def: { en: "Keeping hot experts in VRAM and the rest on CPU or SSD, loaded on demand. It is how a 48B-A3B fits modest hardware, paid for in first-token latency per sequence.", es: "Mantener los expertos calientes en VRAM y el resto en CPU o SSD, cargados bajo demanda. Es como un 48B-A3B cabe en hardware modesto, pagado en latencia del primer token por secuencia." } },
  { id: "shared-experts", module: "mixture-of-experts", term: { en: "shared experts", es: "expertos compartidos" }, def: { en: "Experts every token passes through without consulting the router. They cut variance and take load off the router, at the cost of always being resident.", es: "Expertos por los que pasa cada token sin consultar al router. Reducen la varianza y bajan la carga del router, a costa de estar siempre residentes." } },
  { id: "rope", module: "attention", term: { en: "RoPE", es: "RoPE" }, def: { en: "Rotary position embeddings: encode position by rotating queries and keys by a position-dependent angle. Attention then depends on relative distance, which generalizes to lengths never trained on.", es: "Rotary position embeddings: codifica la posición rotando queries y keys por un ángulo que depende de la posición. La atención pasa a depender de la distancia relativa, que generaliza a longitudes nunca entrenadas." } },
  { id: "rmsnorm", module: "attention-residuals", term: { en: "RMSNorm", es: "RMSNorm" }, def: { en: "Normalizing a vector by its root-mean-square instead of mean and variance. On keys it is not decoration: without it a noisy layer wins the softmax simply by shouting.", es: "Normalizar un vector por su raíz cuadrática media en vez de por media y varianza. En las keys no es adorno: sin ella, una capa ruidosa gana el softmax simplemente por gritar." } },
  { id: "softmax-attention", module: "kimi-delta-attention", term: { en: "softmax attention", es: "atención softmax" }, def: { en: "The mixer you already know: each token scores its query against every key in the window and mixes the values. Quadratic compute, and a cache that grows with the sequence.", es: "El mezclador que ya conoces: cada token puntúa su query contra todas las keys de la ventana y mezcla los values. Cómputo cuadrático, y una caché que crece con la secuencia." } },
  { id: "linear-attention", module: "kimi-delta-attention", term: { en: "linear attention", es: "atención lineal" }, def: { en: "Rewriting the mixer as a recurrent update to a matrix state. It stores a fixed-size matrix rather than a growing list of keys and values, so memory stops tracking sequence length.", es: "Reescribir el mezclador como una actualización recurrente de un estado matriz. Guarda una matriz de tamaño fijo en vez de una lista creciente de keys y values, así que la memoria deja de seguir a la longitud." } },
  { id: "kda", module: "kimi-delta-attention", term: { en: "Kimi Delta Attention", es: "Kimi Delta Attention" }, def: { en: "A linear-attention variant that gates forgetting per channel rather than per head. Each feature dimension can decay at its own rate, instead of the whole state fading together.", es: "Una variante de atención lineal que controla el olvido por canal en vez de por cabeza. Cada dimensión de rasgo puede decaer a su propio ritmo, en vez de desvanecerse todo el estado junto." } },
  { id: "mla", module: "kimi-delta-attention", term: { en: "Multi-head Latent Attention", es: "Multi-head Latent Attention" }, def: { en: "Attention that compresses keys and values into a smaller latent before caching them. In a hybrid stack the KV cache exists only in these layers, which is what keeps long context affordable.", es: "Atención que comprime keys y values en un latente más pequeño antes de cachearlos. En una pila híbrida la caché KV existe solo en estas capas, que es lo que hace asequible el contexto largo." } },
  { id: "clip", module: "multimodal", term: { en: "CLIP", es: "CLIP" }, def: { en: "Two towers, one for images and one for text, trained so that a matching pair lands near each other in a shared space. The trick that let text describe pictures without labels.", es: "Dos torres, una de imagen y una de texto, entrenadas para que un par que se corresponde caiga cerca en un espacio compartido. El truco que dejó al texto describir imágenes sin etiquetas." } },
  { id: "whisper", module: "multimodal", term: { en: "Whisper", es: "Whisper" }, def: { en: "OpenAI's speech model, and the canonical audio recipe: cut into 30-second segments, convert to log-mel spectrograms, run an encoder over them.", es: "El modelo de habla de OpenAI, y la receta canónica de audio: trocear en segmentos de 30 segundos, convertir a espectrogramas log-mel y pasarles un encoder." } },
  { id: "vlm", module: "unsloth-qwen38", term: { en: "VLM (vision-language model)", es: "VLM (modelo visión-lenguaje)" }, def: { en: "A model that takes images or video alongside text in one stack. Unified means the modalities share the language model rather than bolting a captioner onto the front.", es: "Un modelo que toma imágenes o vídeo junto al texto en una sola pila. Unificado significa que las modalidades comparten el modelo de lenguaje, en vez de pegarle un subtitulador por delante." } },
  { id: "thinking-mode", module: "unsloth-qwen38-run", term: { en: "thinking mode", es: "modo thinking" }, def: { en: "A mode where the model emits reasoning before its answer. On a hybrid model it is a different sampling regime, not a prompt style: thinking and instruct ship different defaults.", es: "Un modo en el que el modelo emite razonamiento antes de la respuesta. En un modelo híbrido es un régimen de muestreo distinto, no un estilo de prompt: thinking e instruct traen valores por defecto distintos." } },
  { id: "nvfp4", module: "unsloth-qwen38-run", term: { en: "NVFP4", es: "NVFP4" }, def: { en: "A 4-bit floating-point format with hardware support on NVIDIA Blackwell. Faster than BF16 where the silicon implements it, and no help at all on a card that does not.", es: "Un formato de coma flotante de 4 bits con soporte hardware en NVIDIA Blackwell. Más rápido que BF16 donde el silicio lo implementa, y de ninguna ayuda en una tarjeta que no." } },
  { id: "flops", module: "serving-and-throughput", term: { en: "FLOPs", es: "FLOPs" }, def: { en: "Floating-point operations: the compute a pass costs. Prefill is FLOPs-bound because it processes many tokens at once; decode usually is not.", es: "Operaciones en coma flotante: el cómputo que cuesta una pasada. El prefill está limitado por FLOPs porque procesa muchos tokens a la vez; el decode normalmente no." } },
  { id: "ttft", module: "serving-and-throughput", term: { en: "TTFT (time to first token)", es: "TTFT (tiempo hasta el primer token)" }, def: { en: "How long the user waits before anything appears. Dominated by queueing and prefill, so long prompts push it up — and a cheap price per token does not rescue a four-second spinner.", es: "Cuánto espera el usuario antes de que aparezca nada. Lo dominan la cola y el prefill, así que los prompts largos lo suben — y un precio bajo por token no rescata una rueda girando cuatro segundos." } },
  { id: "tpot", module: "serving-and-throughput", term: { en: "TPOT (time per output token)", es: "TPOT (tiempo por token de salida)" }, def: { en: "The average gap between tokens once generation starts. Dominated by decode, so memory bandwidth, KV cache and concurrency set it, not raw compute.", es: "El hueco medio entre tokens una vez empieza la generación. Lo domina el decode, así que lo fijan el ancho de banda, la caché KV y la concurrencia, no el cómputo bruto." } },
];

export function getTerm(id: string) {
  return GLOSSARY.find((t) => t.id === id);
}

export function localizeTerm(t: GlossaryTerm, locale: Locale) {
  return { id: t.id, module: t.module, term: t.term[locale], def: t.def[locale] };
}
